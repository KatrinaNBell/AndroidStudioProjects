Inside this submission folder is one folder and two zipped programs with a proof video.

The Documents folder contain an Interim report and an Instruction manual (both in pdf form)

The zipped programs contains two projects (each app).

The proof video is a video of the transfer working.
